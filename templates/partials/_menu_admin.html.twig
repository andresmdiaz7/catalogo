
<div class="btn-group" role="group" aria-label="Opciones de cliente">
    {# Botón de administración #}
    <a class="btn btn-outline-dark" href="{{ path('app_admin_panel') }}">
        <i class="bi bi-gear-fill me-2"></i>Administración
    </a>
    {# Botón de cerrar sesión #}
    <a class="btn btn-dark" href="{{ path('app_logout') }}">
        <i class="bi bi-box-arrow-right me-2"></i>Cerrar Sesión
    </a>
</div>
