{% extends 'admin/base.html.twig' %}

{% block title %}Editar Menú{% endblock %}

{% block content %}
    <div class="container-fluid">
        <h1>Editar Menú</h1>

        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-primary">Guardar cambios</button>
            <a href="{{ path('app_admin_menu_index') }}" class="btn btn-secondary">Volver</a>
        {{ form_end(form) }}
    </div>
{% endblock %}